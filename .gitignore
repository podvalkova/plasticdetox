# Local-only social/production assets (never publish to the site repo)
Instagram/
.DS_Store

# Credentials — never commit
plasticdetox-credentials.csv
*credentials*.csv
*.pem
.env

# Social scheduler: channel/board ids, regenerate with `buffer.mjs setup`
tools/social/channels.json

# Social: scraped X candidates and seen ledger stay local (repo is public)
tools/social/x-candidates.json
tools/social/x-seen.json

# Extension build artifacts
dist/
tools/social/x-replies.html
tools/social/x-candidates-archive.json
tools/social/x-rotation.json

# Wrangler local cache and KV state, never publish
worker/.wrangler/
dist/

# wrangler local build cache
.wrangler/
tools/social/reddit-candidates.json
tools/social/reddit-seen.json
tools/social/reddit-replies.html

# App Store Connect identifiers for the TestFlight script (identifiers, not
# the signing key, which stays in ~/.appstoreconnect/private_keys/).
app/.env.local
